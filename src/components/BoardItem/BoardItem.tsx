import React from 'react';
import BoardItemM, { BoardItemProps } from "@cloudscape-design/board-components/board-item";

const BoardItem = (props: BoardItemProps) => {
    return <BoardItemM {...props} />;
};

export default BoardItem;
