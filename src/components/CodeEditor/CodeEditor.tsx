import React from 'react';
import CodeEditorBase, { CodeEditorProps } from '@cloudscape-design/components/code-editor';

const CodeEditor = (props:CodeEditorProps) => {
  return <CodeEditorBase {...props} />;
};

export default CodeEditor;
