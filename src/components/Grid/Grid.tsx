import React from 'react';
import GridBase, { GridProps } from '@cloudscape-design/components/grid';
/**
 * @uxpindocurl https://cloudscape.design/components/grid/
 * @uxpindescription The grid component distributes content on a page. It helps build consistent, balanced, and responsive layouts across an application.
 */
const Grid = (props: GridProps) => {
  return <GridBase {...props} />;
};

export default Grid;
