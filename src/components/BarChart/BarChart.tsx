import React from 'react';
import BarChartBase, { BarChartProps } from '@cloudscape-design/components/bar-chart';

const BarChart = (props:BarChartProps<any>) => {
  return <BarChartBase {...props} />;
};

export default BarChart;
