import React from 'react';
import TextFilterBase, { TextFilterProps } from '@cloudscape-design/components/text-filter';

const TextFilter = (props:TextFilterProps) => {
  return <TextFilterBase {...props} />;
};

export default TextFilter;
