import React from 'react';
import TokenGroupBase, { TokenGroupProps } from '@cloudscape-design/components/token-group';

const TokenGroup = (props:TokenGroupProps) => {
  return <TokenGroupBase {...props} />;
};

export default TokenGroup;
