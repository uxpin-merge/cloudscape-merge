import React from 'react';
import LinkBase, { LinkProps } from '@cloudscape-design/components/link';
/**
 * @uxpindocurl https://cloudscape.design/components/Link/
 * @uxpindescription A link component is an anchor tag that defines a hyperlink, which a user can interact with to find out more information about a concept, task, or field.
 */
const Link = (props: LinkProps) => {
  return <LinkBase {...props} />;
};

export default Link;
