import React from 'react';
import TagEditorBase, { TagEditorProps } from '@cloudscape-design/components/tag-editor';

const TagEditor = (props:TagEditorProps) => {
  return <TagEditorBase {...props} />;
};

export default TagEditor;
