import React from 'react';
import AreaChartBase, { AreaChartProps } from '@cloudscape-design/components/area-chart';

const AreaChart = (props:AreaChartProps<any>) => {
  return <AreaChartBase {...props} />;
};

export default AreaChart;
