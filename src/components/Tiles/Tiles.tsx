import React from 'react';
import TilesBase, { TilesProps } from '@cloudscape-design/components/tiles';

const Tiles = (props:TilesProps) => {
  return <TilesBase {...props} />;
};

export default Tiles;
