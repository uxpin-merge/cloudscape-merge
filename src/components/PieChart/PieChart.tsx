import React from 'react';
import PieChartBase, { PieChartProps } from '@cloudscape-design/components/pie-chart';

const PieChart = (props:PieChartProps) => {
  return <PieChartBase {...props} />;
};

export default PieChart;
