import React from 'react';
import IconBase, { IconProps } from '@cloudscape-design/components/icon';
/**
 * @uxpindocurl https://cloudscape.design/components/icon/
 * @uxpindescription Display basic icons that match with Cloudscape's sizes, colors, and typography.
 */
const Icon = (props: IconProps) => {
  return <IconBase {...props} />;
};

export default Icon;
