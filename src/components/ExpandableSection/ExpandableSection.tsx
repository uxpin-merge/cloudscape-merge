import React from 'react';
import ExpandableSectionBase, { ExpandableSectionProps } from '@cloudscape-design/components/expandable-section';

const ExpandableSection = (props:ExpandableSectionProps) => {
  return <ExpandableSectionBase {...props} />;
};

export default ExpandableSection;
