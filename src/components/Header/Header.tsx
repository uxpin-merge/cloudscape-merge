import React from 'react';
import HeaderBase, { HeaderProps } from '@cloudscape-design/components/header';
/**
 * @uxpindocurl https://cloudscape.design/components/header/
 * @uxpindescription Summarizes the content that's displayed under it and provides a space for optional action buttons.
 */
const Header = (props: HeaderProps) => {
  return <HeaderBase {...props} />;
};

export default Header;
