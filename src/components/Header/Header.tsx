import React from 'react';
import HeaderBase, { HeaderProps } from '@cloudscape-design/components/header';

const Header = (props:HeaderProps) => {
  return <HeaderBase {...props} />;
};

export default Header;
