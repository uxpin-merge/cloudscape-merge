import React from 'react';
import TextContentBase, { TextContentProps } from '@cloudscape-design/components/text-content';

const TextContent = (props:TextContentProps) => {
  return <TextContentBase {...props} />;
};

export default TextContent;
