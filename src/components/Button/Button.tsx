import React from 'react';
import ButtonBase, { ButtonProps } from '@cloudscape-design/components/button';
/**
 * @uxpindocurl https://cloudscape.design/components/button/
 * @uxpindescription Allows users to initiate actions in the user interface.
 */
const Button = (props: ButtonProps) => {
  return <ButtonBase {...props} />;
};

export default Button;
