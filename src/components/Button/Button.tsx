import React from 'react';
import ButtonBase, { ButtonProps } from '@cloudscape-design/components/button';

const Button = (props:ButtonProps) => {
  return <ButtonBase {...props} />;
};

export default Button;
