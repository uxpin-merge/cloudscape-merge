import React from 'react';
import FileInputBase, { FileInputProps } from '@cloudscape-design/components/file-input';

const FileInput = (props:FileInputProps) => {
  return <FileInputBase {...props} />;
};

export default FileInput;
