import React from 'react';
import BreadcrumbGroupBase, { BreadcrumbGroupProps } from '@cloudscape-design/components/breadcrumb-group';

const BreadcrumbGroup = (props:BreadcrumbGroupProps) => {
  return <BreadcrumbGroupBase {...props} />;
};

export default BreadcrumbGroup;
