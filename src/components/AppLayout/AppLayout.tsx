import React from 'react';
import AppLayoutBase, { AppLayoutProps } from '@cloudscape-design/components/app-layout';

const AppLayout = (props:AppLayoutProps) => {
  return <AppLayoutBase {...props} />;
};

export default AppLayout;
