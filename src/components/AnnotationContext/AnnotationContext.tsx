import React from 'react';
import AnnotationContextBase, { AnnotationContextProps } from '@cloudscape-design/components/annotation-context';

const AnnotationContext = (props:AnnotationContextProps) => {
  return <AnnotationContextBase {...props} />;
};

export default AnnotationContext;
