import React from 'react';
import FormFieldBase, { FormFieldProps } from '@cloudscape-design/components/form-field';
/**
* @uxpindocurl https://cloudscape.design/components/form-field/
* @uxpindescription With the form field, users can create properly-styled controls in a form.
*/
const FormField = (props: FormFieldProps) => {
  return <FormFieldBase {...props} />;
};

export default FormField;
