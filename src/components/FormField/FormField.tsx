import React from 'react';
import FormFieldBase, { FormFieldProps } from '@cloudscape-design/components/form-field';

const FormField = (props:FormFieldProps) => {
  return <FormFieldBase {...props} />;
};

export default FormField;
