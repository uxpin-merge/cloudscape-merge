import React from 'react';
import LineChartBase, { LineChartProps } from '@cloudscape-design/components/line-chart';

const LineChart = (props:LineChartProps<any>) => {
  return <LineChartBase {...props} />;
};

export default LineChart;
