import React from 'react';
import LiveRegionBase, { LiveRegionProps } from '@cloudscape-design/components/live-region';

const LiveRegion = (props:LiveRegionProps) => {
  return <LiveRegionBase {...props} />;
};

export default LiveRegion;
